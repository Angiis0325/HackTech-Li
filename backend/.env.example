NODE_ENV=development
PORT=4000

# --- Desarrollo local: estos valores son solo de ejemplo ---
# En producción, corre `npm run generate-secrets` y usa valores reales.
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/hacktech_li
JWT_SECRET=change_this_super_secret_key
JWT_EXPIRES_IN=1d

# En producción DEBE ser el dominio real del sitio web (no "*").
# El servidor rechaza arrancar con NODE_ENV=production y CORS_ORIGIN="*".
CORS_ORIGIN=http://localhost:3000

ADMIN_INVITE_CODE=hacktech2026
N8N_WEBHOOK_TOKEN=change_this_integration_token
N8N_WEBHOOK_URL=
